import * as React from "react";
import { PacmanLoader } from "react-spinners";

export default () => (
  <div className="LoadingSpinner">
    <PacmanLoader color="rgb(31, 62, 113)" size={12} />
  </div>
);
